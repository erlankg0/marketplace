export interface IData{
    id: number,
    imageUrl: string,
    name: string,
    description: string,
    price: number,
    authorImageUrl: string,
    authorFullName: string,
    images?: string[],
}